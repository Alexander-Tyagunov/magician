Common AI mistakes: mixing Options API and Composition API patterns; forgetting .value on refs in script; mutating props directly; not using defineEmits for events.
Commands: dev: `npm run dev`, build: `npm run build`, lint: `npm run lint`.
Gotchas: Vue 3 Composition API uses `ref()` for primitives and `reactive()` for objects; `<script setup>` is the preferred SFC format.
