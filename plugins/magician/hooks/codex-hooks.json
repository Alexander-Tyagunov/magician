{
  "description": "Magician Codex-only Bash safety hook. Uses a separate Codex event-schema adapter so Claude's hook and guard remain independent. This POSIX defense-in-depth hook cannot intercept later write_stdin input and makes no Windows or PowerShell claim.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"${PLUGIN_ROOT}/scripts/codex-destructive-guard.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
