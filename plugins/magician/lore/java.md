Common AI mistakes: null handling without Optional; raw types instead of generics; not closing streams (use try-with-resources); StringBuilder vs string concatenation in loops.
Commands: build: `mvn package` or `gradle build`, test: `mvn test` or `gradle test`.
Gotchas: Records (Java 16+) replace boilerplate POJOs; `var` for local type inference (Java 10+); use `List.of()` for immutable lists.
