Common AI mistakes: using _process for physics (use _physics_process); not freeing nodes causing memory leaks; circular references between nodes; ignoring the scene tree for communication (use signals).
Commands: run: open in Godot editor, export via Project > Export.
Gotchas: Godot 4 uses GDScript 2.0 — type annotations required for performance; signals decouple nodes; use Resources for shared data; @export for inspector-editable properties.
