Common AI mistakes: mutating state directly; missing dependency arrays in useEffect; stale closure capturing old state; calling hooks conditionally; forgetting key prop in lists.
Commands: test: `npm test`, build: `npm run build`.
Gotchas: useCallback/useMemo are not always needed — profile first; React 18 strict mode double-invokes effects in dev only.
