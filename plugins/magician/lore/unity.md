Common AI mistakes: using Find/GetComponent in Update (cache in Start); Coroutine misuse (forgetting yield); not using object pooling for frequent instantiation; blocking the main thread with sync operations.
Commands: build via Unity editor Build Settings; test with Test Runner.
Gotchas: [SerializeField] exposes private fields to Inspector; Time.deltaTime for frame-rate-independent movement; Physics layers separate collision groups; DOTS/ECS for performance-critical systems.
