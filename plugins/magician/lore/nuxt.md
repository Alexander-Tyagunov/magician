Common AI mistakes: confusing pages/ auto-routing with manual Vue Router; using client-only APIs in server-rendered components; forgetting `await` in `useFetch` / `useAsyncData`.
Commands: dev: `npm run dev`, build: `npm run build`.
Gotchas: Nuxt 3 uses auto-imports — no explicit import needed for `ref`, `useFetch`, etc.; `<ClientOnly>` wrapper prevents SSR for browser-only components.
